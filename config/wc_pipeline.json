{
  "_说明": "主流程开关。match_broadcast 接入(2026-06-19 起默认开启,修复'播报漏采'bug):off=只复用已有的赛事播报(单场做过不重做);on=matchday ⑤ 自动为每一场已结算但没做播报的场跑完整条线(fetch_sources抓原文→broadcast_synth合成→归档→嵌两队summary块B)。唯一需agent的是找源URL(WebSearch写进 data_raw/<ts>/_broadcast_urls/<Mid>.json)。配套硬门禁 broadcast_preflight.py 在预测前强制每已结算场都有播报。",
  "auto_run_match_broadcast": true
}
